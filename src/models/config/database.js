import { ArticleModel, NewsMessageModel, MemberModel } from '@/models/index'

export default {
  Articles: {
    model: ArticleModel,
  },
  NewsMessages: {
    model: NewsMessageModel,
  },
  Members: {
    model: MemberModel,
  },
}
